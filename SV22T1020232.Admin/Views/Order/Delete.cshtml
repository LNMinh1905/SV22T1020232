@model OrderViewInfo
@{
    Layout = null;
}
<div class="modal-header text-bg-danger">
    <strong class="modal-title">
        Xóa đơn hàng #@Model.OrderID
    </strong>
    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
</div>

<form id="formOrderDelete" method="post" action="@Url.Action("Delete", "Order", new { id = Model.OrderID })" onsubmit="return handleOrderDelete(event, this);">
    <div class="modal-body">
        <div class="alert alert-danger d-flex align-items-center">
            <i class="bi bi-info-circle me-2"></i>
            Bạn có chắc chắn muốn xóa đơn hàng này? Thao tác không thể hoàn tác.
        </div>
    </div>

    <div class="modal-footer">
        <button type="submit" class="btn btn-danger">
            <i class="bi bi-check"></i> Xóa
        </button>
        <button type="button"
                class="btn btn-secondary"
                data-bs-dismiss="modal">
            Đóng
        </button>
    </div>
</form>
<script>
    function handleOrderDelete(event, form) {
        event.preventDefault();

        const formData = new FormData(form);

        fetch(form.action, {
            method: 'POST',
            body: formData
        })
        .then(response => {
            if (!response.ok) {
                throw new Error('Lỗi khi gọi API');
            }
            return response.json();
        })
        .then(result => {
            if (result.Code === 0) {
                alert(result.Message);
            } else {
                const modalElement = document.getElementById('dialogModal');
                const modalInstance = bootstrap.Modal.getInstance(modalElement);
                if (modalInstance) {
                    modalInstance.hide();
                }
                alert(result.Message || 'Xóa đơn hàng thành công!');
                window.location.href = '/Order';
            }
        })
        .catch(error => {
            console.error('Error:', error);
            alert('Không gửi được yêu cầu');
        });

        return false;
    }
</script>
